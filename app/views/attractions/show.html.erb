<h2><%= @attraction.name %></h2>
<% if can? :edit, Attraction %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% else %>
  <%= form_for @ride do |f| %>
    <%= f.hidden_field :attraction_id, :value => @attraction.id %>
    <%= f.submit 'Go on this ride' %>
  <% end %>
<% end %>
